# Wave A finds — label → implement → PR (klepacze)

- `kkipngenokoech/phoenix` — watcher `ai:ready`/`ai:revise` → plan→code→test→PR; karty: `instances/kkipngenokoech-phoenix/`
- `lennystepn-hue/issue2claude` — Marketplace Action, trigger `claude-ready` → Claude Code → PR (+ fix/rebase); `instances/lennystepn-hue-issue2claude/`
- `dryvist/ai-workflows` — reusable GHA; `ai:ready` / `cc-issue-resolver` / Copilot assign; `instances/dryvist-ai-workflows/`
- `shipshitdev/skills` — `dispatch:claude`/`dispatch:codex` (ex `ready-for-agent`) → executing-plans → QA → PR; `instances/shipshitdev-skills/`
- `chippingway/orchestrator` — local poll + `workflow:*` labels → worktree agents → PR + HITL merge; `instances/chippingway-orchestrator/`
- `nilbuild/claude-queue` — CLI batch z `--label` + etykiety `claude-queue:*` → PR; `instances/nilbuild-claude-queue/`
