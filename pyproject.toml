[project]
name = "lokay"
version = "0.3.0"
description = "Continuous multi-repo issue-to-PR mill built from Unix atoms and Fala."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
  "pyyaml>=6.0",
  "fala",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.uv.sources]
fala = { path = "../Fala", editable = true }

[project.scripts]
lokay = "lokay.cli:main"
# Atomic processes (one job each)
lokay-list-issues = "lokay.proc.list_issues:main"
lokay-list-inbox = "lokay.proc.list_inbox:main"
lokay-get-issue = "lokay.proc.get_issue:main"
lokay-select-issue = "lokay.proc.select_issue:main"
lokay-triage-issue = "lokay.proc.triage_issue:main"
lokay-intake-check = "lokay.proc.intake_check:main"
lokay-intake-issue = "lokay.proc.intake_issue:main"
lokay-issue-split = "lokay.proc.issue_split:main"
lokay-label-issue = "lokay.proc.label_issue:main"
lokay-stage-label = "lokay.proc.stage_label:main"
lokay-reap-stale-implementing = "lokay.proc.reap_stale_implementing:main"
lokay-close-issue = "lokay.proc.close_issue:main"
lokay-assign-issue = "lokay.proc.assign_issue:main"
lokay-make-branch = "lokay.proc.make_branch:main"
lokay-worktree-add = "lokay.proc.worktree_add:main"
lokay-plan-issue = "lokay.proc.plan_issue:main"
lokay-localize = "lokay.proc.localize:main"
lokay-relocalize-off-goal = "lokay.proc.relocalize_off_goal:main"
lokay-run-agent = "lokay.proc.run_agent:main"
lokay-commit-all = "lokay.proc.commit_all:main"
lokay-assert-real-diff = "lokay.proc.assert_real_diff:main"
lokay-test-local = "lokay.proc.test_local:main"
lokay-rebase-onto-base = "lokay.proc.rebase_onto_base:main"
lokay-push = "lokay.proc.push_branch:main"
lokay-pr-create = "lokay.proc.pr_create:main"
lokay-pr-label = "lokay.proc.pr_label:main"
lokay-list-prs = "lokay.proc.list_prs:main"
lokay-pr-checks = "lokay.proc.pr_checks:main"
lokay-pr-route = "lokay.proc.pr_route:main"
lokay-pr-review = "lokay.proc.pr_review:main"
lokay-pr-merge = "lokay.proc.pr_merge:main"
lokay-pr-close = "lokay.proc.pr_close:main"
# Graph (Fala) + composers
lokay-run-path = "lokay.proc.run_path:main"
lokay-factory-pass = "lokay.compose.factory:main"
lokay-factory-tick = "lokay.proc.factory_tick:main"
lokay-factory-begin = "lokay.proc.factory_begin:main"
lokay-host-ff = "lokay.proc.host_ff:main"
lokay-survey-repos = "lokay.proc.survey_repos:main"
lokay-survey-prs = "lokay.proc.survey_prs:main"
lokay-survey-inbox = "lokay.proc.survey_inbox:main"
lokay-survey-ready = "lokay.proc.survey_ready:main"
lokay-plan-pass = "lokay.proc.plan_pass:main"
lokay-dispatch-triage = "lokay.proc.dispatch_triage:main"
lokay-dispatch-closeout = "lokay.proc.dispatch_closeout:main"
lokay-resolve-conflicts = "lokay.proc.resolve_conflicts:main"
lokay-closeout-pr = "lokay.proc.closeout_pr:main"
lokay-closeout-prs = "lokay.proc.closeout_prs:main"
lokay-refresh-occupancy = "lokay.proc.refresh_occupancy:main"
lokay-reap-stale-worktrees = "lokay.proc.reap_stale_worktrees:main"
lokay-select-implement = "lokay.proc.select_implement:main"
lokay-queue-conflict = "lokay.proc.queue_conflict:main"
lokay-dispatch-implement = "lokay.proc.dispatch_implement:main"
lokay-compute-health = "lokay.proc.compute_health:main"
lokay-record-pass = "lokay.proc.record_pass:main"
lokay-preflight = "lokay.proc.preflight:main"
lokay-daemon = "lokay.proc.daemon:main"
lokay-recovery-begin = "lokay.proc.recovery_begin:main"
lokay-recovery-mill = "lokay.proc.recovery_mill:main"
lokay-recovery-observe = "lokay.proc.recovery_observe:main"
lokay-recovery-record = "lokay.proc.recovery_record:main"
lokay-recovery-incident = "lokay.proc.recovery_incident:main"
lokay-recovery-run-self-repair = "lokay.proc.recovery_run_self_repair:main"
lokay-self-repair-prepare = "lokay.proc.self_repair_prepare:main"
lokay-self-repair-validate = "lokay.proc.self_repair_validate:main"
lokay-self-repair-push-main = "lokay.proc.self_repair_push_main:main"
lokay-self-repair-activate = "lokay.proc.self_repair_activate:main"
lokay-self-repair-preflight = "lokay.proc.self_repair_preflight:main"
lokay-self-repair-close = "lokay.proc.self_repair_close:main"
lokay-repos = "lokay.proc.repos_list:main"
lokay-repos-clone-missing = "lokay.proc.repos_clone_missing:main"
lokay-issue-to-pr = "lokay.compose.issue_to_pr:main"
lokay-pr-repair = "lokay.compose.pr_repair:main"
lokay-pr-triage = "lokay.compose.pr_triage:main"
lokay-tick = "lokay.compose.tick:main"
lokay-mill = "lokay.compose.mill:main"
lokay-status = "lokay.compose.status:main"
lokay-yield-report = "lokay.proc.yield_report:main"
lokay-wake = "lokay.proc.wake:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lokay"]

[tool.hatch.build.targets.wheel.force-include]
"src/lokay" = "lokay"

[tool.lokay]
# Repository-declared local verifier for lokay-test-local. Missing this table
# is an honest skip — the mill must not invent `uv run --extra dev pytest`.
test = ["uv", "run", "--extra", "dev", "pytest", "-q"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "tests"]
